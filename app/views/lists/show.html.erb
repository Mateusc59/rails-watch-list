  <div class="container">

  </div>

<div class="container">

<h2>LIST:<%= @list.name %></h2>
<% @list.movies.each do |movie| %>
  <%= movie.title %>
<%end%>
<%= link_to "return to lists ", lists_path %>

<%= link_to "+ add movie to the list", new_list_bookmark_path(@list) %>
  </div>
</div>
